#### 获取当前工作路径和py文件名

- 采用pathlib库

```python
from pathlib import Path
import os.path
import sys

    print("当前工作目录路径: {}".format(os.getcwd()))
    print("当前工作目录路径: {}".format(os.path.abspath('.')))

    print("当前文件的绝对路径: {}".format(sys.argv[0]))
    print("当前文件名: {}".format(os.path.basename(sys.argv[0])))
    print("当前文件名: {}".format(os.path.basename(__file__)))
    print("当前文件名：{}".format(Path(__file__).name))

    folder, filename = os.path.split(__file__)
    print("当前工作目录路径: {}".format(folder))
    print("当前工作文件名: {}".format(filename))
    
# 打印内容： 
当前工作目录路径: E:\Projects_Py\auto_test_1214\auto_test\common
当前工作目录路径: E:\Projects_Py\auto_test_1214\auto_test\common
当前文件的绝对路径: E:/Projects_Py/auto_test_1214/auto_test/common/log_set.py
当前文件名: log_set.py
当前文件名: log_set.py
当前文件名：log_set.py
当前工作目录路径: E:/Projects_Py/auto_test_1214/auto_test/common
当前工作文件名: log_set.py
```



#### 获取当前目录路径、子目录、子文件

```python
import os

def file_name(file_dir):
    for root, dirs, files in os.walk(file_dir):
        print(root) #当前目录路径
        print(dirs) #当前路径下所有子目录
        print(files) #当前路径下所有非目录子文件
```



#### python查找文件中某个字符串出现的次数

```python
'''查找文件中某个字符串总共出现的次数'''
def findStr(str):
    with open("filePath",'r') as f:
        counts = 0
        for line in f.readlines():
            time = line.count(str)
            counts += time
        print("%s出现的次数：%d"%(str,counts))

findStr("str")
```





#### 清空文件&清空文件夹
清空文件：
```python
    # 清空文件
    with open(filename, 'w') as f1:
        f1.seek(0)
        f1.truncate()
```
删除文件：
```python
    # 删除文件
    if os.path.exists(filename):
        # 可使用以下两种方法。
        os.remove(filename)
        #os.unlink(filename)
	else:
    	print 'no such file:%s'%filename
```
清空文件夹：

```python
import shutil  
shutil.rmtree('要清空的文件夹名')  
os.mkdir('要清空的文件夹名')  
```

